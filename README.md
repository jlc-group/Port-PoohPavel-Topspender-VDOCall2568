# 🎯 Lucky Draw Customer Portal

ระบบ Lucky Draw สำหรับลูกค้าตรวจสอบสิทธิ์ Top Spender และ VDO Call ผ่านเว็บไซต์

## ฟีเจอร์หลัก

- 🔍 **ค้นหาลูกค้าด้วยเบอร์โทรศัพท์** - ค้นหาง่ายๆ ด้วยเบอร์ 10 หลัก
- 👤 **การปกป้องข้อมูลส่วนตัว** - แสดงข้อมูลบางส่วนเพื่อความปลอดภัย
- 🎁 **แสดงสิทธิ์ Top Spender (ซอง)** - จำนวนสิทธิ์ลุ้นรางวัล
- 📹 **แสดงสิทธิ์ VDO Call (หลอด)** - สิทธิ์โทรปรึกษาพิเศษ
- 📱 **Responsive Design** - รองรับมือถือและคอมพิวเตอร์
- 🇹🇭 **ภาษาไทยเต็มรูปแบบ** - อินเตอร์เฟซภาษาไทย 100%

## โครงสร้างโปรเจค

```
Port-PoohPavel-TopSpender&VDOCall2568/
├── server.js                 # Express server + API endpoints
├── google-sheets-example.js  # Google Sheets integration
├── public/
│   └── index.html           # Frontend HTML/CSS/JS
├── package.json             # Dependencies
├── .env.example             # Environment variables template
└── README.md               # This file
```

## การติดตั้งและใช้งาน

### 1. ติดตั้ง Dependencies

```bash
npm install
```

### 2. ตั้งค่า Environment Variables

คัดลอกไฟล์ `.env.example` เป็น `.env` และตั้งค่า:

```bash
cp .env.example .env
```

### 3. เริ่มต้นระบบ

สำหรับ Development:
```bash
npm run dev
```

สำหรับ Production:
```bash
npm start
```

### 4. เข้าใช้งาน

เปิดเว็บบราวเซอร์ที่: `http://localhost:3000`

## API Endpoints

### Health Check
```
GET /api/health
```

### ค้นหาลูกค้าด้วยเบอร์โทรศัพท์
```
GET /api/customer/search/:phone
```

### ดูข้อมูลสิทธิ์ของลูกค้า
```
GET /api/customer/:id/rights
```

## การเชื่อมต่อ Google Sheets

ระบบเชื่อมต่อกับ Google Sheets ID: `1PA4KRWqZFSeI8R88-8_S4ZhvayJBugHOZboevVo9cMg`

### ฟิลด์ที่ต้องการใน Google Sheets:

- `user_id` - ID ของลูกค้า
- `name` - ชื่อจริง
- `surname` - นามสกุล
- `email` - อีเมล (ถ้ามี)
- `telephone` - เบอร์โทรศัพท์
- `tickets_top_spender` - จำนวนสิทธิ์ Top Spender
- `tickets_vdocall` - จำนวนสิทธิ์ VDO Call

## ฟีเจอร์รายละเอียด

### ความปลอดภัยข้อมูล
- **Data Masking**: แสดงเฉพาะชื่อและตัวอักษรแรกของนามสกุล
- **Phone Masking**: แสดงเบอร์โทรแบบ xxx-xxxxx67
- **No Full Data**: ไม่แสดงข้อมูลส่วนตัวทั้งหมด

### User Experience
- **Fast Search**: ค้นหาได้ทันที
- **Loading States**: แสดงสถานะระหว่างโหลด
- **Error Handling**: แจ้งข้อความผิดพลาดในภาษาไทย
- **Mobile First**: ออกแบบให้ใช้งานง่ายบนมือถือ

### Tab Interface
- **Top Spender Tab**: แสดงสิทธิ์ประเภทซอง 🎁
- **VDO Call Tab**: แสดงสิทธิ์ประเภทหลอด 📹

## การปรับแต่ง

### การเปลี่ยนสีธีม
แก้ไข CSS ใน `public/index.html`:
```css
.header h1 {
    color: #764ba2; /* เปลี่ยนสีตามต้องการ */
}
```

### การเปลี่ยนข้อความ
แก้ไขข้อความใน HTML tags:
```html
<h1>🎯 Lucky Draw 2568</h1>
```

## Dependencies

- `express` - Web server framework
- `cors` - Cross-origin resource sharing
- `dotenv` - Environment variables
- `axios` - HTTP client (สำหรับ future features)
- `google-spreadsheet` - Google Sheets API integration
- `nodemon` - Development auto-restart

## ข้อมูลสำหรับผู้พัฒนา

### เพิ่มฟีเจอร์ใหม่
1. เพิ่ม API endpoints ใน `server.js`
2. แก้ไข frontend ใน `public/index.html`
3. ทดสอบการทำงาน

### การดีบัก
```bash
# ตรวจสอบการเชื่อมต่อ Google Sheets
npm test

# ตรวจสอบ API
curl http://localhost:3000/api/health
```

## License

MIT License - สามารถนำไปใช้งานได้ฟรี

## ติดต่อ

สำหรับข้อสงสัยหรือต้องการปรับแต่งเพิ่มเติม กรุณาติดต่อทีมพัฒนา